node_modules/
data/*.sqlite*
uploads/
tmp-test/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
certs/
dist/

# Handed out once, then deleted — never commit real credentials
dist/temporary-passwords.csv
dist/d1-import.sql

# secrets and local state — SESSION_SECRET lives in Cloudflare, never here
.env
.env.*
!.env.example
.dev.vars*
.wrangler/
*.jks
*.keystore
# the release signing key's passwords — back up with the .jks, never commit
android/keystore.properties
# the published APK and its apk.json: built by scripts/build-apk.mjs, deployed
# from this folder, too large to belong in git
public/downloads/
*.log
android/.idea/
